# MacGregor PLC Atlas

Загрузите в корень GitHub-репозитория всё содержимое архива, включая папки `data` и `assets`.

GitHub Pages: Settings → Pages → Deploy from a branch → main → /(root).

Нельзя загружать только index.html: приложение использует app.js, styles.css, JSON и изображения.
